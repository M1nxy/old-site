{
  "name": "m1nxd3v",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate && echo m1nx.dev > dist/CNAME",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup",
    "publish": "npm run generate && npm run deploy"
  },
  "dependencies": {
    "core-js": "^3.25.3",
    "nuxt": "^2.15.8",
    "vue": "^2.7.10",
    "vue-server-renderer": "^2.7.10",
    "vue-template-compiler": "^2.7.10"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^5.3.3",
    "daisyui": "^2.6.0",
    "postcss": "^8.4.17",
    "push-dir": "^0.4.1"
  }
}
